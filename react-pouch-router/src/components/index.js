'use strict';

import React from 'react';
import BillList from './bill-list';

export default React.createClass({
  render() {
    return (
      <div>
        <BillList />
      </div>
    );
  }
});
